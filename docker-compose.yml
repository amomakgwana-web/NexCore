# Auto-generated by scripts/generate-infra.mjs from services/manifest.json.
# Run: docker compose up --build
# Requires a .env file with SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY (see .env.example).
services:
  auth-service-1:
    build: ./services/auth-service
    container_name: auth-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  auth-service-2:
    build: ./services/auth-service
    container_name: auth-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  payroll-service-1:
    build: ./services/payroll-service
    container_name: payroll-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  payroll-service-2:
    build: ./services/payroll-service
    container_name: payroll-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  billing-service-1:
    build: ./services/billing-service
    container_name: billing-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  billing-service-2:
    build: ./services/billing-service
    container_name: billing-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  accounting-service-1:
    build: ./services/accounting-service
    container_name: accounting-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  accounting-service-2:
    build: ./services/accounting-service
    container_name: accounting-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  finance-service-1:
    build: ./services/finance-service
    container_name: finance-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  finance-service-2:
    build: ./services/finance-service
    container_name: finance-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  hr-service-1:
    build: ./services/hr-service
    container_name: hr-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  hr-service-2:
    build: ./services/hr-service
    container_name: hr-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  crm-service-1:
    build: ./services/crm-service
    container_name: crm-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  crm-service-2:
    build: ./services/crm-service
    container_name: crm-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  procurement-service-1:
    build: ./services/procurement-service
    container_name: procurement-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  procurement-service-2:
    build: ./services/procurement-service
    container_name: procurement-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  approvals-service-1:
    build: ./services/approvals-service
    container_name: approvals-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  approvals-service-2:
    build: ./services/approvals-service
    container_name: approvals-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  claims-service-1:
    build: ./services/claims-service
    container_name: claims-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  claims-service-2:
    build: ./services/claims-service
    container_name: claims-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  assets-service-1:
    build: ./services/assets-service
    container_name: assets-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  assets-service-2:
    build: ./services/assets-service
    container_name: assets-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  contracts-service-1:
    build: ./services/contracts-service
    container_name: contracts-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  contracts-service-2:
    build: ./services/contracts-service
    container_name: contracts-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  projects-service-1:
    build: ./services/projects-service
    container_name: projects-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  projects-service-2:
    build: ./services/projects-service
    container_name: projects-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  notifications-service-1:
    build: ./services/notifications-service
    container_name: notifications-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  notifications-service-2:
    build: ./services/notifications-service
    container_name: notifications-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  audit-service-1:
    build: ./services/audit-service
    container_name: audit-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  audit-service-2:
    build: ./services/audit-service
    container_name: audit-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  vault-service-1:
    build: ./services/vault-service
    container_name: vault-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  vault-service-2:
    build: ./services/vault-service
    container_name: vault-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  entities-service-1:
    build: ./services/entities-service
    container_name: entities-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  entities-service-2:
    build: ./services/entities-service
    container_name: entities-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  budget-service-1:
    build: ./services/budget-service
    container_name: budget-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  budget-service-2:
    build: ./services/budget-service
    container_name: budget-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  payments-service-1:
    build: ./services/payments-service
    container_name: payments-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  payments-service-2:
    build: ./services/payments-service
    container_name: payments-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  state-service-1:
    build: ./services/state-service
    container_name: state-service-1
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  state-service-2:
    build: ./services/state-service
    container_name: state-service-2
    environment:
      - PORT=4000
      - SUPABASE_URL=${SUPABASE_URL}
      - SUPABASE_SERVICE_ROLE_KEY=${SUPABASE_SERVICE_ROLE_KEY}
      - GATEWAY_URL=http://gateway:8080
    restart: unless-stopped
    networks: [nexcore-net]
  gateway:
    image: nginx:1.27-alpine
    container_name: nexcore-gateway
    ports:
      - "8080:8080"
    volumes:
      - ./gateway/nginx.docker.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - auth-service-1
      - auth-service-2
      - payroll-service-1
      - payroll-service-2
      - billing-service-1
      - billing-service-2
      - accounting-service-1
      - accounting-service-2
      - finance-service-1
      - finance-service-2
      - hr-service-1
      - hr-service-2
      - crm-service-1
      - crm-service-2
      - procurement-service-1
      - procurement-service-2
      - approvals-service-1
      - approvals-service-2
      - claims-service-1
      - claims-service-2
      - assets-service-1
      - assets-service-2
      - contracts-service-1
      - contracts-service-2
      - projects-service-1
      - projects-service-2
      - notifications-service-1
      - notifications-service-2
      - audit-service-1
      - audit-service-2
      - vault-service-1
      - vault-service-2
      - entities-service-1
      - entities-service-2
      - budget-service-1
      - budget-service-2
      - payments-service-1
      - payments-service-2
      - state-service-1
      - state-service-2
    restart: unless-stopped
    networks: [nexcore-net]

networks:
  nexcore-net:
    driver: bridge
